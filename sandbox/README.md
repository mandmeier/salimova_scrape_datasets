## sandbox

This folder contains legacy / experimental scripts kept temporarily to preserve earlier work.

They are **not** the recommended entrypoints for reproducing results in a journal context.

Use the Make targets documented in the top-level `README.md` instead.

